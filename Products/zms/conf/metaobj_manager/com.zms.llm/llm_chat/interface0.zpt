<tal:block 
    tal:define="zmscontext options/zmscontext" 
    tal:replace="structure python:zmscontext.getBodyContent(request)">
    Use this block to embed a frontend chat that uses the configured 
    ZMSLLMConnector provider.
</tal:block>