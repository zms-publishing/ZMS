# User and group IDs
UID=1000
GID=1000

# Zope/ZMS paths
HOME_DIR=/home/zope
VENV_DIR=${HOME_DIR}/venv

INSTANCE_DIR=${HOME_DIR}
# For utilizing a local path as SRC_MOUNT
SRC_DIR=${VENV_DIR}/src/zms
SRC_MOUNT=${HOME_DIR}/src/zms-publishing/ZMS
# add a volumes entry in the docker-compose.yml and set the path here
# volumes:
#   - ${SRC_MOUNT}/:${SRC_DIR}/:rw