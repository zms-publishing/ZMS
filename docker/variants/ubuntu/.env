# User and group IDs
UID=1000
GID=1000

# Zope/ZMS paths
HOME_DIR=/home/zope
VENV_DIR=${HOME_DIR}/venv

INSTANCE_DIR=${HOME_DIR}
SRC_DIR=${VENV_DIR}/src/ZMS
SRC_MOUNT=${HOME_DIR}/src/zms-publishing/ZMS5